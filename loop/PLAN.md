# Reflection King 自主 Loop — 验证闭环 + 真实站点爬取测试

**目标(用户 2026-06-30)**:核心智能 = 验证闭环。找真实站点(100-200)触发验证、本地靶场练习、从 GitHub/web 学习解决逻辑。一边计划一边实现一边 workflow(ultracode/全 opus)。慢节奏跑 2-3 小时起步,自我提升。

**loop**: cron `0310d89d` 每 30m 心跳 + workflow 完成自动唤醒。

---

## 北极星
给任意 URL,系统能**自己判断怎么抓**(routing,已有)+ **核实抓到的能不能播**(verification,缺失)。补上 verification 这下半场。

## 现状(已读核心代码)
- `extractors/mod.rs:190-266` resolve 链:Auto 跑全链不短路、按原始 score 排序、truncate(80)。**全程零网络验证,候选永远 Untested**。
- `models.rs:858` `CandidateValidationState` 有 8 态(Usable/NeedsProfile/SuspectAd/Expired/Drm/RegionBlocked/Failed)**但无代码迁移**。
- 已有 smoke harness(`scripts/smoke/vps_run_catalog_smoke.sh` + python)外部探测 media,记录 status/content_type/content_range → 这就是要内化进 resolver 的验证信号。

## 基础设施
- home-cloud `/home/dwgx_user/reflection-king-smoke`,HEAD c063be1(含已推送的 4 安全 fix 16ea4ab 的内容)
- 服务 :8780,dashboard rk.dwgx.top
- 代理:sing-box 已装;配置写本地 gitignored(凭据勿回显)
- 推送链:home-cloud 改→编译验证→format-patch→D:→git am→push master

---

## 阶段计划(交替:计划→实现→workflow→记录)

### P0 基础(本轮)
- [ ] 代理配上(sing-box + 用户给的 vless/vmess),验证 egress IP 变化 → 解决数据中心 IP 反爬
- [ ] 跑一遍现有 catalog smoke 拿基线(多少 pass/fail/各 ErrorClass 分布)
- [ ] 建 loop 状态文件:RESULTS.md(每轮 pass/fail)、LEARNINGS.md(失败模式+解法)

### P1 验证 stage 设计(核心)
- [ ] 设计:Untested→{Usable|Expired|RegionBlocked|Failed|Drm} 状态机
- [ ] 探测策略:HEAD / Range bytes=0-1 GET,**必走 policy_http SSRF 守卫**,看 status/content-type/content-length/Accept-Ranges
- [ ] 安全短路:有候选验证为 Usable 才停 browser(绝不在 Untested 上 break)— 闭合审计「链不短路」
- [ ] 排序改造:Usable > Untested 猜测 — 闭合「top-80 截断」「validation_state 词汇空转」

### P2 实现 + 真实站点迭代
- [ ] workflow 实现 verification stage(opus 起草+评审,串行编译)
- [ ] 扩充 catalog 到 100-200(从现有 + web 检索公开媒体站)
- [ ] 跑 → 收集失败 → GitHub/web 学习(yt-dlp/各 extractor 的解法)→ 改进 → 再跑

### P3 学习沉淀
- [ ] 每轮把新失败模式 + 解法写 LEARNINGS.md 和 memory
- [ ] 把可泛化的识别/验证逻辑回灌到 generic + verification

---

## 进度日志
- 2026-06-30 启动:读完核心代码,确认 verification 闭环是杠杆点。loop 调度。
- 2026-06-30 ✅ 代理配好(task#13):sing-box vmess 出口 38.246.238.26(vless-reality EOF 不通),socks5:1080/http:1081,YouTube 经代理 200。
- 2026-06-30 ⏳ 基线运行中(task#14):live_smoke.py 跑 stable-platform catalog(all tiers),summary 写 loop-evidence/baseline-stable-*.json。smoke harness 已记录 status/content_type/content_range —— 验证信号的现成来源。
- 关键发现:`scripts/smoke/live_smoke.py` 是功能完整的探测器(可单 URL `--url` 或 catalog),输出 media_checks(status/content_type/content_range/bytes)。这正是要内化进 resolver 的验证逻辑——可直接参考它的判定。
- 2026-06-30 心跳#2:① design workflow 第一次因 `parallel([agent()])` 语法挂(应 `parallel([()=>agent()])`),已修+重跑(wf_d1979c0d-dd8),进行中。② 基线 401 排查:**admin-key-drift 真实发生**——C1 部署时 rebuild 容器导致 `/data/admin-key.txt`(64b 旧)与活跃 key 漂移;**我刚推的 WARN 修复在生产正确触发**(日志 `RK_API_KEY does not match any active admin key (drift after container rebuild?) configured_key=loc***(len=19)`)= 4 个修复之一的实战验证 ✅。活跃 key 是 `/tmp/rk_key.txt`(25b)。③ 用对 key 后基线正常跑(peertube resolving→remuxing)。
- 2026-06-30 心跳#3:① **基线完成**(task#14 done):stable-platform 15/15 ready,12 有效 206/video-mp4(详见 RESULTS.md)。代理实证有效——bilibili/niconico/youku/X/reddit 全解出。② **重大发现:candidate_count=0 全员**——都走外部 yt-dlp 路径直接产 artifact、不经 MediaCandidate 表 → verification stage 只对 generic/browser/direct(产 candidate)路径有意义,外部路径无 candidate 可验。已注入 design。③ **design workflow 第二次挂**:用了 `agent({label,prompt})` 对象形式(我 memory 里早记过这坑却没看!)→ 所有 subagent 收到 `[object]` 空任务秒退。已重写为 `agent(promptString,{label,agentType:'general-purpose'})` 正确签名,新 run wf_5b2203db-1ac 起来了(agent 48k/80k/108k bytes 真在干活)。
- 教训(已记 memory):写 workflow 前**先读** [[workflow-agent-api-signature]]——两个坑(positional-string prompt + general-purpose agentType)都记过,这次仍栽 parallel-thunk + object-prompt。
- 2026-06-30 心跳#4:**VERIFICATION-DESIGN.md 完成**(303 行,wf_5b2203db-1ac,4 agent/219k tok/16min)。code-grounded、implementation-ready。核心:① probe primitive(verify.rs 新文件)照搬 hanime.rs:42-84 的 redirect::none()+逐跳 validate_url SSRF 范式,**故意走 .no_proxy() 不经 sing-box**(验证服务端→媒体源真实可达性,代理是抓取面的事);② 幂等 verify_top_n(top_n=8,JoinSet 并发),两个调用点(browser 前 gate + 循环后 pass)全 job 最多 N 探;③ 状态机 classify_probe 纯函数(410→Expired/451→RegionBlocked/403+geo→RegionBlocked/403+login→NeedsProfile/200+tiny-html→SuspectAd/206 media→Usable/manifest body-parse→Drm);④ 短路仅在 ≥1 Usable,绝不 Untested break;⑤ 排序 state_tier(Usable>NeedsProfile>SuspectAd>Untested>RegionBlocked>Drm>Expired>Failed)+score,truncate 前验证;⑥ **零 schema 迁移**(8 字段已存在);⑦ flag RK_VERIFY_ENABLED 默认 OFF。
- 待决(design 留给 owner):is_expensive() trait 方法需加到 SourceExtractor;5xx→Failed 不重试(延后);Untested 排在确证坏态之上(故意);默认 OFF。
- **下一步 = P1 实现**(task#15):走 ultracode workflow 在 home-cloud 全树编译实现 verify.rs + resolve() 接入 + 单测,据 PLAN 推送链 format-patch→D:→git am→push。
- 2026-06-30 心跳#5:**✅ verification stage 实现完成并合 master(be7c3bf)**(task#15 done)。
  - 新 `crates/reflection-core/src/extractors/verify.rs`(710 行):`probe()` SSRF-safe(照搬 hanime redirect::none()+逐跳 validate_url+validate_response_url_and_peer,`.no_proxy()`);`classify_probe()` 纯函数状态机;`verify_one()` 回填 8 字段;`verify_top_n()` 幂等限 top_n;`has_usable()`/`sort_verified()`/`state_tier()`;`VerifyConfig::from_env()`。
  - `mod.rs` 接入:resolve() 循环里 expensive extractor(browser_probe/yt_dlp/you_get/lux/streamlink)前短路 gate(仅 ≥1 Usable break,绝不 Untested)+ 循环后幂等 pass + state_tier 排序(替换 raw-score)+ truncate(cfg.truncate_max)。`is_expensive_extractor()` 按 name 判定。
  - **编译干净,15 个新单测 + 全 suite 80 passed 0 failed**(resolver 三测无回归)。
  - 修正点:① validation_state 是 `Option<>` 且 direct 设 `Some(Untested)`→`is_unverified` 匹配 None+Untested;② status 是 u16 非 i64;③ MediaCandidate 无 Default→测试用全字段 helper;④ browser 名是 `browser_probe`;⑤ VerifyConfig 走 env 不改 ExtractContext(减 blast radius,记为偏离设计)。
  - **flag RK_VERIFY_ENABLED 默认 OFF → 行为零变化**,安全。三处源码已对齐(D:/home-cloud/GitHub master)。
- **下一步 = P1.5 live 验证(task#16)**:rebuild docker → RK_VERIFY_ENABLED=1 → 跑产 candidate 的 generic/direct catalog(基线全 external 路径 candidate_count=0,验证价值体现不出)→ 实证 Untested→Usable 迁移 + 短路省时 + 排序提前。
- 2026-06-30 心跳#6:**✅ live 端到端验证通过(task#16 done),master=4beb9b2**。绕开 docker rebuild,直接加 `#[ignore]` live 集成测试驱动真实 probe 路径(home-cloud 直连 CDN,probe 本就 `.no_proxy()`)。**4/4 live 测试通过**,实证验证闭环真在真实站点工作:
  - 3 个 mp4(blender/w3schools/archive.org,含 302→dn801203.us.archive.org 跟随)→ **Usable**(200/206 video/mp4)。
  - HLS(mux x36xhzz.m3u8)+ DASH(akamai bbb.mpd)body-parse → **Usable**。
  - 过期签名 URL(expires=过去)→ **Expired,零网络**(status=None,pre-probe 短路)。
  - 排序实证:可达 mp4(raw score 1)验证后**压过** bogus host(score 999)→ Usable 居首。**这就是「top-80 原始分截断」修复的活证据**。
  - **抓到并修了一个真 bug**:`expires=1000000001` 也是合法 hex,原 `parse_expiry_epoch` 先试 hex→读成 4147 年(未来)→跳过 Expired 去探不可达 host。生产里 `expires=1700000000` 这种纯十进制 epoch 会同样误判。修复:先试十进制,hex 仅对 0x 前缀/含字母值生效。+2 regression 单测。
  - 全 suite **82 passed 0 failed**(+2 expiry 回归),5 ignored(live)。flag 仍默认 OFF。
- **可选后续(未做,非阻塞)**:full-server docker rebuild + RK_VERIFY_ENABLED=1 端到端,验证 worker 把 validation_state 写库 + dashboard 展示。但闭环逻辑已 live 证实,这层是集成包装。
- **下一步 = P2 真实站点迭代**:扩 catalog 到 100-200 产 candidate 的站点(generic/direct 路径,非 external yt-dlp),跑验证收集失败模式 → GitHub/web 学解法 → 回灌 generic+verify。
- 2026-06-30 心跳#7:**✅ full-chain live 验证通过(task#17 推进),master=2f15245**。加 `chain_live` 集成测试:真实页 → `GenericExtractor` scan → candidates → verify stage 探测 → 排序,端到端。`RK_VERIFY_ENABLED=1 cargo test chain_live -- --ignored`。**1/1 通过,3 页共 5 candidate 发现+验证**:
  - **抓到真实陷阱(LEARNINGS L1)**:Sintel 页 `archive.org/embed/.../sintel-2048-stereo.mp4` URL 以 .mp4 结尾、URL-pattern 打分高(74),但探测发现返回 `text/html`(是播放器页非媒体)→ 降为 **SuspectAd**;真 `download/.../*.mp4` 流确认 **Usable(80)** 排第一。**纯分数会选错 embed 页,验证后选对**——这就是验证闭环的核心价值实证。
  - 选站经验(LEARNINGS L2):archive.org/details(og:video+twitter:player+VideoObject,最可靠)、wikimedia commons(bare .webm)能静态抓出;w3schools/ted/媒体站 JS 壳抓不出。
  - 全 suite 仍绿(87 filtered,chain_live 1 passed)。建 `loop/LEARNINGS.md`。
- **下一步 = P2 续**:① 把 archive.org/details + wikimedia 扩成正式 catalog(几十条)批量跑 chain_live 收集更多失败模式;② 关注 manifest(m3u8/mpd)在真实站点的 Drm/解析失败;③ 误判模式(真流被判 SuspectAd?signed-URL 站的 Expired 边界)记 LEARNINGS 回灌 classify_probe。
- 2026-06-30 心跳#8:**✅ 抓修签名 manifest 真 bug(task#17 推进),master=6eeea69**。recon 真实 manifest(unified-streaming/apple/akamai/axprod ClearKey)时发现:verify 用 `final_url.ends_with(".m3u8")` / `candidate.url.ends_with(".mpd")` 判 manifest——对**含 ?query 的原始 URL** 做后缀匹配。**签名 manifest(`master.m3u8?token=...`,CDN 常态)不匹配 → 不抓 body → DRM 检测被静默跳过 → 保护流被放行为 Usable**(LEARNINGS L3)。修:新增 `url_path_has_manifest_ext()` 解析 URL 取 path 判扩展名、忽略 query/fragment,两处调用点统一用。3 regression 测试(签名 HLS+SAMPLE-AES→Drm / 签名 DASH→Usable / helper 单元),全 suite **85 passed 0 failed**。
  - 边界观察(记 L3):BODY_HEAD_CAP=16KB,超大 manifest 的 ContentProtection 若落在 16KB 后仍会漏判;当前样本都在 cap 内,待真实样本触发再改。
  - 泛化:任何按 URL 扩展名分流的逻辑都该解析 path 而非 raw-string 后缀(signed URL 是生产常态)。direct.rs 的 classify_extension 值得照此审一遍。
- **下一步 = P2 续**:① 审 direct.rs `classify_extension` 是否也犯 raw-string 后缀匹配的错(signed 直链 mp4?token=...);② 把 archive.org/details 扩成正式 catalog 批量跑 chain_live 收集更多失败模式;③ 找带 signed-URL/manifest 的真实站点验证 L3 修复在真实流量上的效果。
- 2026-06-30 心跳#8 续:审 direct.rs `extension()` —— **干净**,它已用 `ctx.url.path()`(解析后取 path、自动去 query),从无 raw-string bug。L3 漏洞仅 verify.rs(操作 `candidate.url`/`outcome.final_url` 这两个 String 字段)。唯一站点已修。
- **下一步 = P2 续**:① 把 archive.org/details 扩成正式 catalog(几十条)批量跑 chain_live 收集更多失败模式;② 找带 signed manifest 的真实站点验证 L3 修复在真实流量效果;③ manifest 16KB body cap 边界:找超大 master manifest 样本看 DRM 是否漏判。
- 2026-06-30 心跳#9:**✅ 抓修两个 HLS master-playlist DRM 漏判(task#17 推进),master=1d3e3e3**(LEARNINGS L4)。深挖 manifest 正确性时发现 `classify_manifest` 只查 `#ext-x-key`+字面厂商词,两个真实 DRM HLS 漏判为 Usable:**(a)** RFC 8216 规定 master playlist 用 `#EXT-X-SESSION-KEY` 宣告 DRM(media 才用 `#EXT-X-KEY`),而 verify **先探 master**,子串 `"ext-x-key"` 不含于 `"ext-x-session-key"`(实测 False)→ 受保护 master 落 Usable;**(b)** 真实 Widevine HLS(shaka angel-one)只用 `KEYFORMAT` system UUID 标识,无字面 "widevine"。修:`has_key_tag` 加 SESSION-KEY,`names_drm_system` 加 Widevine(edef8ba9-)+PlayReady(9a04f079-)UUID。3 regression(master SESSION-KEY→Drm / UUID→Drm / 干净 master→Usable),全 suite **88 passed 0 failed**。
  - 泛化:DRM 检测要覆盖协议所有层级标签(HLS KEY vs SESSION-KEY)+所有标识方式(厂商词 vs system UUID)。只匹配最常见一种 = 受保护流被当可播。
- **下一步 = P2 续**:① 把 archive.org/details 扩成正式 catalog(几十条,advancedsearch.php API 取 feature_films,过滤 147543b error 页)批量跑 chain_live;② manifest 16KB body cap 边界:超大 master 的 DRM 标记落 cap 后是否漏判(可对 manifest 单独提高 cap);③ DASH 侧 `<ContentProtection>` 各 scheme 覆盖确认(目前子串够稳)。
- 2026-06-30 心跳#10(/loop 重启,目标 100-200 站):**✅ 建 catalog 批量 harness + 抓修瞬时 5xx 误判 Failed(master=71e67dc)**(LEARNINGS L5)。① harvest 41 页 archive catalog(advancedsearch.php,逐个验证 og:video)写 `loop/catalog-archive.txt`;② 新增 `chain_live_catalog` 测试读 `RK_CHAIN_CATALOG` 文件跑全链 + per-state 统计,**catalog 扩到 100-200 站无需重编译**;③ 批量基线 41/41 产 candidate,40/41 top-Usable,states {Usable:66,SuspectAd:6,Failed:2};④ 诊断唯一 Failed(ElephantsDream)= archive download/ 302 到分片存储节点 dnNNNN 瞬时 500,几分钟后 200。修:`is_transient_status`(429/5xx)+ verify_one retry 1 次(definitive 4xx 不 retry)。全 suite 89 passed。
- **下一步 = P2 续(扩品类找新失败模式)**:archive 单源同质化(全 og:video+download mp4)。① catalog 加 HLS/DASH demo 站(bitmovin/shaka/unified-streaming/apple devstreaming/dash-if)验 manifest 路径(Drm/SESSION-KEY/UUID 真实命中);② 加 wikimedia commons 文件页 + JSON-LD VideoObject 新闻站 + JS 壳站(预期零候选,验 generic 边界);③ manifest 16KB body cap 边界;④ 用 ultracode workflow 扇出并行 harvest 多品类站点凑到 100-200。
- 2026-06-30 心跳#11:**✅ manifest 品类 catalog + 抓修 HLS variant DRM 漏判(master=924202c)**(LEARNINGS L6)。① 建 `catalog-manifest.txt`(7 真实 HLS/DASH demo 流)跑批量,验 L3/L4 修复在生产流量:5 Usable + 2 Drm(axprod ClearKey + shaka widevine),DRM 检测路径全部真实命中;② 诊断 shaka angel-one master 误判 Usable——HLS-Widevine 的 key 在 variant playlist 不在 master,verify 只探 master 漏判。修:`verify_one` 对 Usable HLS master 做**一层变体下降**(取第一个 #EXT-X-STREAM-INF 后 variant、probe + classify,Drm 则升级)。live 验证 shaka → Drm,5 干净流保持 Usable,无误升。全 suite 90 passed。
- **下一步 = P2 续**:① **扇出 ultracode workflow 并行 harvest 多品类站点凑 100-200**(wikimedia commons 文件页 / JSON-LD VideoObject 新闻站 / 更多 HLS-DASH demo / JS 壳站验 generic 零候选边界);② manifest 16KB body cap 边界(超大 master variant 下降也受 cap 限);③ DASH 也可考虑 variant 类比下降(Representation 层 ContentProtection,但 MPD 通常自带顶层 ContentProtection,优先级低)。
- 2026-06-30 心跳#12:**✅ ultracode workflow 4 路扇出 harvest + 150 站规模批量验证(达成 100-200 目标)**(LEARNINGS L7)。① 开 workflow(`harvest-workflow.js`,run wf_0436cd92)4 个 general-purpose subagent 经代理验证真实 URL:archive 新集合(40)/streaming demo HLS-DASH(38,8 带 DRM)/JSON-LD+JS壳(25);wikimedia 第 4 路卡在 Commons API 两步解析超 20min 未返回(长尾 harvester 教训);② 手动合并 3 路去重成 `catalog-mixed.txt`(102 新 URL,+existing 48 = ~150 站);③ 批量跑:90/102 产候选,81 top-Usable,states {Usable:131,SuspectAd:11,NeedsProfile:10,Drm:8,Failed:4};④ 12 零候选全是预期 JS 壳/SPA(twitter/ig/tiktok/...),generic 不误报;JSON-LD 路径 TED 11 页全 Usable(contentUrl→hls.ted.com 真 200);⑤ 核查 octet-stream manifest 非 bug(generic 靠 URL 扩展名发现)。**无新代码改动——本轮是大规模正确性确认**。
- **下一步 = P3(管线已在 150 真站验证稳)**:① 重跑/拆小 wikimedia harvest(Commons API 分批,设硬超时)补全第 4 品类,把 catalog 推到 180-200;② manifest 16KB body cap 边界(超大 master + variant 下降双重受限);③ NeedsProfile(10 例)状态来源核查——哪些站触发、profile 机制是否合理;④ 把 mixed catalog 沉淀为 repo 内固定回归集(或文档化跑法),供后续 verify 改动回归。
- 2026-06-30 心跳#13:**✅ harness per-state 可观测性 + 修 .ogv 漏分类(真 bug #6)+ 修复 origin 测试构建断裂(基础设施)**(LEARNINGS L8)。① catalog harness 加"top 候选非 Usable 终态"分组打印 URL,把 mixed catalog 聚合 breakdown 变可操作清单;② 据此定位 archive DaffyDuck 误判 SuspectAd = `classify_url` 缺 `.ogv`/`.ogm`(Ogg video)分支落 None 被当 embed follow,修后 live → top Usable(4 候选),单测 classify_url_ogg_family;③ **诊断中发现 GitHub origin/master(80a167b)cargo test 编译失败**——逐文件 patch 链漏传创建 generic.rs 的基础 commit c063be1,mod.rs 引用了 origin 上不存在的 GenericExtractor,`cargo build` 跳过 test 故一直没暴露。从 home-cloud format-patch c063be1 整 commit + 2 新 commit 一起 git am→D: 现 cargo test 编过 91 passed→推 origin(e6a3933),home-cloud 与 origin tree 完全一致。
- **下一步 = P3 续**:① **流程兜底**:每次推后在 D: 跑 `cargo test --no-run`(非仅 build),或全树 sha1 指纹比对,防再漏基础 commit;② 把 mixed catalog(102 页)沉淀为 repo 内固定回归集 + 文档化跑法;③ 重跑/拆小 wikimedia Commons harvest(分批+硬超时)补第 4 品类推到 180-200;④ NeedsProfile 10 例虽非 top 但可抽查机制是否合理;⑤ manifest 16KB body cap 边界。
- 2026-06-30 心跳#14:**✅ 候选级诊断可观测性 + 修 403/S3-AccessDenied 误判 NeedsProfile(真 bug #7)**(LEARNINGS L9)。① harness 加候选级(非仅页 top)诊断 URL 清单,定位 mixed catalog 的 10 个 NeedsProfile 全是 TED py.tedcdn.com fallback MP4(二级候选,页 top 是 hls manifest Usable);② curl 证这些 fallback 403+S3 `<Error><Code>AccessDenied</Code>` body、Referer/Origin 不解锁=私有对象无 profile 可救;③ 修 classify_probe 加 access-denied signal→Failed + **关键**把 HEAD→Range-GET fallback 扩到 401/403(否则 HEAD 无 body 分类器看不到 XML),单测 2 个,全 suite 93 passed,live TED fallback 由 NeedsProfile→Failed;④ 推前 D: 跑 cargo test --no-run 落实 L8 流程兜底,推 origin(d06616d),home-cloud 与 origin tree 一致。累计真 bug 修复=7。
- **下一步 = P3 续2**:① **新品类扩 catalog**:拆小批跑 wikimedia Commons API harvest(分批+硬超时,避免之前的卡死)补第 4 品类,把 catalog 推到 180-200;② manifest 16KB body cap 边界(超大 master + variant 下降双重受限)——找/造一个 variant 数超 16KB 的 master 验证截断不丢 DRM 信号;③ 把 mixed catalog(102 页)沉淀为 repo 内固定回归集 + 文档化跑法;④ SuspectAd 11 例候选级核查(同 L9 方法,看是否有可救的误判)。
- 2026-06-30 心跳#15:**✅ SuspectAd 候选级核查(无误判)+ 修单次 transient retry 不足致假 Failed(真 bug #8)**(LEARNINGS L10)。① harness 诊断纳入 SuspectAd 并加 `[HTTP状态 ct]` 前缀;② SuspectAd 11 例核查:10 个是 archive.org/embed player 页(200 text/html,GenericExtractor 按路径采了 iframe player URL)正确落 SuspectAd,1 个 .ogg 真媒体边界——桶内无可救误判;③ 顺带挖出 Failed 从 5 暴涨 17 含 3 个 archive download 500,curl 证 charlie_chaplin 连 500,500,206=多节点 failover,单次 retry 不够→改有界循环 TRANSIENT_RETRIES=2 + 300ms*attempt 退避,live 3 页全恢复 top=Usable,全 suite 93 passed;④ 推前 D: cargo test --no-run,推 origin(4a2227f),tree 一致。累计真 bug 修复=8。
- **下一步 = P3 续3**:① **新品类扩 catalog 到 180-200**:拆小批跑 wikimedia Commons API harvest(分批+硬超时);② manifest 16KB body cap 边界——造/找 variant 列表超 16KB 的 HLS master 验证 DRM 信号(#EXT-X-KEY 在 16384 字节后)不被截断漏检(已读 classify_manifest+BODY_HEAD_CAP=16KB+Range bytes=0-16383,理论风险存在,需实证);③ 把 mixed catalog 沉淀为 repo 内固定回归集 + 文档化跑法;④ .ogg(application/ogg)在 SuspectAd 的边界——是否该 Usable(Ogg 是合法媒体容器)。
- 2026-06-30 心跳#16:**✅ 修 application/ogg 漏判媒体(真 bug #9)+ manifest 16KB body cap 截断 DRM 硬化(安全短路)**(LEARNINGS L11)。① content_type_is_media 不认 application/ogg(Ogg 容器),archive .ogg(200 application/ogg+OggS magic)真媒体落 SuspectAd→加 application/ogg+x-ogg,live 该页 1 SuspectAd→4/4 Usable;② 实测 axprod Manifest_1080p.mpd=18561 字节超 16KB cap(DRM 标记偏移 935-5577 靠前,截断后仍判 Drm 纯运气),长 HLS master variant 级 #EXT-X-KEY 可能排 16KB 后→截断漏检放行受保护流→probe 拆 probe_capped,manifest 用 MANIFEST_BODY_CAP=512KiB,媒体/error-body 仍 16KB,单测 drm_tag_past_16kib_still_detected,live axprod 仍 Drm/Apple master 仍 Usable;③ 全 suite 95 passed,推前 D: cargo test --no-run,推 origin(30171e4),tree 一致。累计真 bug 修复=9。
- **下一步 = P3 续4**:① **新品类扩 catalog 到 180-200**:拆小批跑 wikimedia Commons API harvest(分批+硬超时);② 把 mixed catalog(102 页)沉淀为 repo 内固定回归集 + 文档化跑法(回归集是后续 verify 改动的安全网,优先级升);③ 真实长 HLS 多码率 master(variant 级 DRM 排 16KB 后)找一个实样验证 512KiB cap 的实战效果(本轮是合成单测+理论实样,缺真站命中);④ image/* 在媒体白名单是否过宽(图片当视频候选?)抽查。
- 2026-06-30 心跳#17:**✅ mixed catalog(102 页)沉淀为 repo 回归集 + 修测试 cwd 相对路径坑**(LEARNINGS L12)。① 三个 harvest catalog(archive 41/manifest 7/mixed 102)+ loop/README.md(跑法+基线)提交进 repo loop/——此前只活在 /tmp 重启即丢;② 跑全量发现 cargo test cwd=crate 目录非 repo 根,相对路径 panic ENOENT→改 $PWD/ 绝对路径,mod.rs doc-comment 错误示例(从没真跑过)一并修;③ 全量基线 {Usable:168,Drm:8,SuspectAd:10,Failed:15},90/102 页有候选,81 top=Usable,12 零候选=JS壳社交站,耗时 564s;④ 推前 D: cargo test --no-run,推 origin(517aa23),tree 一致。累计真 bug 修复=9(本轮流程/基础设施)。
- **下一步 = P3 续5**:① **新品类扩 catalog 到 180-200**:wikimedia Commons API harvest(分批+硬超时)补第 4 真品类,加进 catalog-mixed 或新建 catalog-commons.txt;② 真实长 HLS 多码率 master(variant 级 DRM 排 16KB 后)找实样验 512KiB cap 实战(缺真站命中);③ image/* 媒体白名单是否过宽抽查(Video 候选探测回 image/* 是否该降级);④ Failed 15 里逐条候选级核查(同 L9/L10 方法),分离真死 vs 瞬时,看有无新可救误判类型。
- 2026-06-30 心跳#18:**✅ 修持久 429 被当终态 Failed 假死(真 bug #10)+ wikimedia Commons 第 4 品类 catalog**(LEARNINGS L13)。① 用 Commons File: 描述页测 generic,发现 upload.wikimedia.org 变体的 429 落进通配 4xx→终态 Failed;② 实证根因:直接 HEAD 200 video/webm(资源在),burst 复现 429 无 Retry-After ~2-3s 恢复,300/600ms 退避太短清不掉限流窗口;③ 两段修复 verify.rs:429/503 秒级冷却退避(RATE_LIMIT_BACKOFF_MS=2500 或尊重 Retry-After 上限 8s,ProbeOutcome 加 retry_after_secs)+ 持久 429 归 SuspectAd 非 Failed,单测 persistent_429_is_suspect_not_failed,全 suite 96 passed,live Alice.ogv {Failed:2,SuspectAd:1,Usable:3}→{SuspectAd:3,Usable:3};④ 建 catalog-commons.txt(10 页,均验在线 200),全量 47 页基线 {Usable:27,SuspectAd:188,Failed:2}(SuspectAd 主体=429 节流正确非终态,2 Failed=cross-wiki 真 404);⑤ 复犯 L12 陷阱:手加 2 个"知名"标题(Popeye/Chaplin)实为 404,已剔;⑥ 推前 D: cargo test --no-run,推 origin(catalog db4ac97 + fix 5bacef0),三端 tree 一致。累计真 bug 修复=10。
- **下一步 = P3 续6**:① 真实长 HLS 多码率 master(variant 级 DRM 排 16KB 后)找实样验 512KiB cap 实战(缺真站命中,仍欠);② image/* 媒体白名单是否过宽抽查(Video 候选探测回 image/* 是否该降级);③ 429 退避量级 vs 单页 burst:当前 6 候选顺序探测一个 host 仍触发节流,可考虑 per-host 探测间隔/串行节流(但会拖慢 verify,权衡);④ catalog-mixed 全量 Failed 15 里逐条候选级核查,分离真死 vs 瞬时,看有无新可救误判类型。
- 2026-06-30 心跳#19:**✅ image/* 响应不能确认 Video/Audio 候选(verify 边界硬化,非观察 bug)**(LEARNINGS L14)。① 审 image/* 白名单过宽:`content_type_is_media` kind 无关,Video/Audio 候选探测回 image/* 在 verify.rs:418 被判 Usable;② 风险路径=generic.rs:572 JSON-LD VideoObject 无扩展名 contentUrl fallback Video kind,若实为 poster→verify 确认为可播流(安全网该拦的 poster 泄漏);③ **诚实标注**:catalog 无 live 复现(grep image/ 全 0),是潜在硬化非观察误判,**不计真 bug(仍=10)**;④ 修成 kind-aware:image/* 只确认 Image 候选,Video/Audio 降 SuspectAd,octet-stream/ogg 家族不变,单测 2 个全 suite 98 passed;⑤ commons 3 页跑 top 漂移单页隔离证明是 429 节流方差非 guard 误伤;⑥ 推前 D: cargo test --no-run,推 origin(ba8edf0),三端 tree 一致。
- **下一步 = P3 续7**:① 真实长 HLS 多码率 master(variant 级 DRM 排 16KB 后)找实样验 512KiB cap 实战(缺真站命中,长期欠,优先级升——可考虑造一个本地 fixture 或找 bitmovin/dash-if 长 master);② 429 退避 vs 单页 burst:6 候选顺序打一个 host 仍触发节流,考虑 per-host 探测最小间隔(权衡 verify 时延);③ catalog-mixed 全量 Failed 逐条候选级核查分离真死 vs 瞬时;④ verify 其他 kind-aware 盲点排查(audio/* 确认 Video?manifest CT 确认 Image?——同 L14 方法审匹配是否够具体)。
- 2026-06-30 心跳#20:**✅ Manifest-kind 候选在 CT/path 双失配时漏 DRM-parse(verify DRM 旁路硬化,非观察 bug)**(LEARNINGS L15)。① 接 L14 follow-up #4 审 kind-aware 盲点,发现 classify_probe manifest 闸(verify.rs:398)只看 CT+path-ext 不看 kind;② 旁路=mac_cms/hanime 用 url.contains(".m3u8") 赋 Manifest kind 标记可落 query string + manifest 偶服务为 octet-stream(L7 shaka),双失配跳过 classify_manifest→octet-stream 媒体臂→Usable,DRM parse 从不跑(L3/L11 同旁路家族:取了 body 没解析);③ **诚实标注**:demo catalog 无 live 复现(URL 都在 path),潜在硬化**不计真 bug(仍=10)**;④ 修=闸加 kind==Manifest 对齐 verify.rs:556 fetch 口径,单测 2 个(octet-stream DRM→Drm / 干净→Usable)全 suite 100 passed,manifest catalog 不变(5U 2Drm);⑤ 推前 D: cargo test --no-run,推 origin(47c2d04),三端 tree 一致。
- **下一步 = P3 续8**:① 真实长 HLS 多码率 master(variant 级 DRM 排 16KB 后)验 512KiB cap 实战(长期欠,可造本地 fixture 跑——优先级最高);② 429 退避 vs 单页 burst per-host 最小间隔(权衡 verify 时延);③ catalog-mixed 全量 Failed 逐条候选级核查分离真死 vs 瞬时;④ kind-aware 盲点继续:audio/* 确认 Video 候选是否该降级(L14 只挡 image,audio↔video 跨界尚未审——但两者都可播,可能合理,需先想清语义);⑤ 把 mac_cms/hanime 的 `url.contains(".m3u8")` 改成 path-aware 判定(根因侧修,query 里的 .m3u8 误判 kind 是上游隐患,verify 侧已兜底但上游也该准)。
- 2026-06-30 心跳#21:**✅ catalog-mixed 全量 102 页 Failed/SuspectAd 候选级三角验证:零误判**(LEARNINGS L16,无代码改动)。① 接两轮推测性硬化后回实证核心,全量基线 {Usable:163,Drm:8,SuspectAd:10,Failed:20}(552s);② Failed 20 全正确/瞬时:13 TED 私有 fallback 403+XML(L9)+ 3 example.com 字面占位符 URL 404 + 4 archive 500 瞬时节点 failover(L10);③ archive 500 三角验证:HEAD 探 302 从不 500,跟随+Range 8/8 全 206 即时恢复,且 4 页 top 全=Usable(500 命中冗余变体非获胜候选,零用户影响)→不调 TRANSIENT_RETRIES;④ SuspectAd 10 全是 archive.org/embed 播放器页 200 text/html(正确,次级候选);⑤ 结论=verify 管线分类全对,本轮纯实证确认。
- **下一步 = P3 续9**:① 真实长 HLS 多码率 master 验 512KiB cap 实战(长期欠,优先级最高,下轮造本地 fixture:写一个 >512KiB 的 master.m3u8 + variant 含 #EXT-X-KEY DRM 排尾,验 cap 不截断 DR;用 wiremock 或本地 http server);② kind-aware 盲点继续 audio↔video 跨界语义(先想清:Video 候选回 audio/* 是音轨缺视频还是合理纯音频?);③ mac_cms/hanime 的 `.contains(".m3u8")` 改 path-aware(上游根因,verify 已兜底但上游该准);④ 扩 catalog 新品类(欧洲/日韩公共视频站、PeerTube 实例)做 generic 跨站压力测试。
- 2026-06-30 心跳#22:**✅ 真 bug #11:`binary/octet-stream` 未列媒体白名单→PeerTube 真实 MP4 误判 SuspectAd**(LEARNINGS L17,bug 计数 10→**11**)。① 512KiB e2e 因 SSRF guard 封 loopback 无 test 旁路不可行+收益低,转实证 item ④;② 建 PeerTube catalog(SepiaSearch API 取 35 URL 跨 24 联邦实例)跑 chain_live,发现 content_type_is_media(verify.rs:146)收 application/octet-stream 但漏 binary/octet-stream;③ 坐实真媒体:Range 探 206+1.45GB+ftypiso5 magic;④ 修=白名单加该别名,单测 binary_octet_stream_video_is_usable,全 suite 101 passed;⑤ live 复跑 SuspectAd 8→0 Usable 62→70 每页 top 仍 Usable;⑥ catalog-peertube.txt 入库(test asset),三端同步 83f30a7。
- **下一步 = P3 续10**:① 跑完整 PeerTube 35 页(本轮只跑 12)+ 看 Failed 10 的 `[- video/mp4]` HLS fragmented shard 无状态是否瞬时(burst 下 segment 端点 reset?out-of-band 复探);② MIME 别名审计扩面——既然 binary/octet-stream 漏了,系统查 content_type_is_media + content_type_is_manifest 所有精确匹配项的等价别名(如 `application/mp4`、`video/mp4` 变体、manifest 的 `text/vnd.apple.mpegurl`?);③ 扩更多站型(欧洲/日韩公共站、Nico 类、Vimeo)继续找基础设施多样性触发的 gap——这是本轮最高 ROI 路径;④ 512KiB cap e2e 仍欠(SSRF 约束下考虑给 url_policy 加 `#[cfg(test)]` 放行 loopback 或抽纯函数测截断算术,低优先级);⑤ kind-aware audio↔video 语义 + mac_cms `.contains` path-aware(上游根因)。
- 2026-06-30 心跳#23:**✅ 真 bug #12:两复合根因致 Range-忽略/无-HEAD CDN 真实媒体误判 Failed**(LEARNINGS L18,bug 计数 11→**12**)。① 承 L17①跑全 35 页(上轮 12)发现 Failed 33 大量是无-status 的 fragmented.mp4 shard;② out-of-band 坐实:HEAD code=000 超时(端点不实现 HEAD)+ GET 200 但无视 Range 流 38MB;③ 根因A=`response.bytes()` 全量缓冲再截断,无视 Range 的源读爆 8s timeout→改流式 chunk() 到 cap 即停;④ 根因B=媒体先探 HEAD,HEAD 超时(Err)直奔 Failed,原 GET 兜底只覆盖"HEAD 成功但无用"→改首探非-manifest HEAD 遇非-policy Err 回落 GET;⑤ 同 35 页复跑 Failed 33→4 Usable 182→211 每页 top 仍 Usable,残留 4 Failed 全是正确的真 404/500;全 suite 101 passed;⑥ 三端同步 6cf4df4。
- **下一步 = P3 续11**:① **MIME/能力别名审计**(L17+L18 都指向此类)——系统过 content_type_is_media/is_manifest 精确匹配项找漏的别名(`application/mp4`、manifest 的 `text/vnd.apple.mpegurl`/`audio/x-mpegurl`?),**但守 L14/L15 纪律:无 live repro 的别名添加是推测性硬化不计真 bug**,优先用 catalog 实证触发;② **继续扩站型找基础设施多样性 gap**(本轮最高 ROI 已验证两次)——欧洲/日韩公共站、Nico 类、Vimeo、Odysee/LBRY、Rumble,目标 catalog 上 100-200 站;③ tube.tinfoil-hat.net 的 404 m3u8 + infiniteloop 500——确认是真死(deleted/server err)非可救,已正确分类,无需改;④ 512KiB cap e2e 仍欠(低优先级,SSRF 约束);⑤ kind-aware audio↔video 语义 + mac_cms `.contains` path-aware(上游根因);⑥ **可考虑 ultracode workflow 扇出**:并行 N 个 opus subagent 各负责一个站型品类的 catalog 构建+chain_live 跑+核验(读 [[workflow-agent-api-signature]] 避坑)。
- 2026-06-30 心跳#24:**✅ workflow 扇出扩 catalog 两个新品类(无 bug,纯回归网扩面)**。① 首次用 ultracode workflow(`loop/discover-catalog.js`,5 opus subagent 各管一站型:hls-dash-drm/odysee-lbry/vimeo-embed/eu-jp-kr/archive-audio,WebSearch+WebFetch 实证后才收录)——读 [[workflow-agent-api-signature]] 对照三坑一次写对(thunk-parallel + positional-string prompt + general-purpose),5 agent 全返结果共 69 URL(53 validated);② 落两个最高价值 catalog:**catalog-drm-vectors.txt(15)**——Apple/Unified/axprod/shaka/dashif 规范向量跨 6 CDN,chain_live **11 clear→Usable + 5 加密→Drm 零误判**,verify 还抓出 discovery agent 误标 clear 的 cbcs manifest(verify 比 agent 准);**catalog-audio.txt(10)**——librivox/78rpm/MLK + megaphone.fm 播客 CDN,纯 AUDIO kind,全 top=Usable,唯一 SuspectAd 是正确 demote(archive embed 页 text/html);③ DRM 向量稳定不腐烂=最强永久 DRM 回归 fixture;④ README catalog 表补 peertube+drm+audio 三行;⑤ 三端同步 5d8d0c4。
- **下一步 = P3 续12**:① **继续 workflow �there扩站型**——本轮 not-validated 的 16 个(arte/nhk-world/kbs/rtve/yle 等欧日韩,US infra 抓不到但代理可能能)值得挂代理实跑确认;Rumble/Bilibili/niconico/Twitch-VOD 等新形状还没碰;目标 catalog 累计上 100-200 真站;② odysee/vimeo/dailymotion 这批 watch 页本轮**只发现未跑 chain_live**——下轮挂代理跑,看 generic 能否从 player-config JSON(非 og:video)提取候选,这是与 PeerTube/archive 不同的提取路径,可能暴露 generic.rs 的 gap;③ MIME 别名审计(守纪律,catalog 实证优先);④ 512KiB cap e2e(低优先级);⑤ kind-aware + mac_cms path-aware 上游根因。
- 2026-06-30 心跳#25:**✅ 真 bug #13:反爬 401/403 throttle 误判 NeedsProfile(应 SuspectAd)**(LEARNINGS L20,bug 计数 12→**13**)。① 承#24② 跑 catalog-playerconfig.txt(player-config 站:vimeo/streamable/dailymotion/odysee)发现 5 个 Odysee 页 top=NeedsProfile;② 探:`player.odycdn.com/api/v3/streams/free/...mp4`(路径含 **free**=公开免登内容)401 "this content cannot be accessed at the moment",UA-only 仍 401,加 Referer 翻 **429 "Try again later"**,3 连探稳定——是反爬/限流软拒非登录墙;③ 根因=classify_probe 的 401/403 臂在无 geo/access-denied/login signal 时默认落 NeedsProfile(verify.rs:391),但免登内容没有 profile 可补;④ 修=加 `body_has_transient_throttle_signal`(cannot be accessed at the moment / try again later / too many requests / rate limit / temporarily unavailable),在 login signal 之前判,映射 SuspectAd(同持久-429 路径 bug#10);⑤ live 复跑 5 页 NeedsProfile→SuspectAd 每页仍解析,残留 4 NeedsProfile 是 streamable 的 403 签名 URL 备选(各页 top 仍 Usable,正确排序在下,另案);⑥ 单测 +2(101→103 passed);⑦ catalog-playerconfig.txt 入库;⑧ 三端同步 32103bc。**附带发现(非 bug,记下轮)**:vimeo/dailymotion **0 候选**——generic.rs 提不出 player-config JSON 的媒体,已知 gap。
- **下一步 = P3 续13**:① **generic.rs player-config JSON 提取**——vimeo(`config` JSON 内 `progressive`/`hls` URL)、dailymotion(`metadata` JSON 的 `qualities`)0 候选是真 gap,值得查 yt-dlp 的提取逻辑回灌 generic;但**先确认这是否在产品 scope 内**(可能这些站本就不支持,0 候选=正确),别造需求;② streamable 残留 4 NeedsProfile——其 403 备选 mp4 带 Expires/Signature,是过期签名 URL 还是签名 URL 反爬?探 body 看是否 S3 AccessDenied(应 Failed)还是别的;③ 继续扩站型(欧日韩 not-validated 16 个挂代理跑、Rumble/niconico/Twitch-VOD);④ MIME 别名审计(守纪律);⑤ 512KiB cap e2e(低优先级);⑥ kind-aware + mac_cms path-aware 上游根因。
- 2026-06-30 心跳#26:**✅ 真 bug #14:sniff 出的 URL 尾部带 JSON 转义反斜杠致签名 URL 误 403→NeedsProfile**(LEARNINGS L21,bug 计数 13→**14**)。① 承#25② 查 streamable 残留 4 NeedsProfile:候选 `cdn-cf-east.streamable.com/.../theimm.mp4?Expires=...&Signature=...&Key-Pair-Id=...` **Expires=Jul3 2026 未过期**,clean 版 curl=**200 video/mp4 24MB**,但 chain_live 候选尾部多一个 `\`(`...Key-Pair-Id=APKAIEYUVEN4EVB2OKEQ\`)→403 text/xml `<Error><Code>MissingKey</Code>`;② 二分坐实:同 URL 带尾 `\`=403、去掉=200,反斜杠破坏末尾 query param;③ 根因=`url_like_tokens`(scan_bare_urls 的 sniff)在 escaped-JSON 上下文 `\"contentUrl\":\"https://…\"` 里 URL 被 JSON 闭引号 `\"` 终止,scanner 在 `"` 处停但**吞掉了前面的转义 `\`**,`decode_url_escapes` 处理 `\/`+`\uXXXX` 但不管裸尾 `\`,坏 URL 作为独立候选与 clean 版并存;④ 修=token 末尾 `trim_end_matches('\\')`(裸反斜杠永非合法 URL 字节,中段 `\/`/`&` 仍留给 decode);⑤ live 复跑 streamable NeedsProfile 4→0 每页仍 Usable(theimm 2→1 cands、tlr26 6→3),全 catalog breakdown {SuspectAd:5,Usable:4}(原 {NeedsProfile:4,SuspectAd:5,Usable:4})无回归;⑥ 单测 +1(103→104);⑦ 三端同步 8b748e3。
- **下一步 = P3 续14**:① **generic.rs player-config JSON 提取**(承上)——vimeo/dailymotion 0 候选仍未碰,**先确认 scope**(这些站可能本就不在支持列表,0 候选=正确而非 bug)再决定是否回灌 yt-dlp 式提取;② **escaped-JSON URL 提取审计**——既然 sniff 路径吞了尾 `\`,系统查 scan_bare_urls 之外是否还有别处把 escaped-JSON URL 提取得不干净(JSON-in-JSON、`/` 路径分隔);decode_url_escapes 的覆盖是否完整(`&` 大小写、`&#38;` 等)有 catalog 实证再补;③ 继续扩站型(欧日韩 not-validated 16 个挂代理、Rumble/niconico/Twitch-VOD)目标 catalog 100-200 站;④ MIME 别名审计(守纪律);⑤ 512KiB cap e2e(低优先级);⑥ kind-aware + mac_cms path-aware 上游根因。
- 2026-06-30 心跳#27:**✅ 关闭 #26① scope 问题(vimeo/dailymotion 0 候选=正确非 bug),无代码改动本节**。① **坐实**:vimeo/dailymotion 都在 `PlatformHint` SUPPORTED 枚举里(models.rs:596/598)且 host 分发已配(main.rs:2167/2174),但生产 `DiscoveryMode::Auto` 链是 `direct→hanime1→mac_cms→generic→**yt_dlp**→you_get→lux→streamlink→browser_probe`(mod.rs:451-462)——这俩 JS-SPA player 由 **yt_dlp** 兜底,**不是 generic 的职责**;② **关键**:chain_live 测试 harness 只接 `direct+generic`(mod.rs:574-577,故意不挂 yt_dlp 避免外部依赖),所以 catalog 里 vimeo/dailymotion 报 0 候选是 harness 覆盖盲区,不是提取缺陷——往 generic 里塞 player-config 解析会与 yt_dlp 重复造轮子,**否决回灌**;③ 诚实记账:本节是 scope 澄清(防造需求),非修 bug,bug 计数仍 **14**;④ 转向更高 ROI:跑 catalog-mixed(102 站)找新误判。
- **下一步 = P3 续15**:① **catalog-mixed(102 站)verify 结果分析**——本节后台已起跑,下轮读 /tmp/mixed.log 的 breakdown + diag_candidates,挑 NeedsProfile/Failed/Drm/Expired 的可疑项二分坐实(守 L14/L15:无 live repro 不改码);② **escaped-JSON URL 提取审计**(承#26②,catalog 实证优先);③ 扩站型(欧日韩挂代理、Rumble/niconico/Twitch-VOD);④ MIME 别名审计(守纪律);⑤ 512KiB cap e2e(低优先级);⑥ kind-aware + mac_cms path-aware 上游根因。**注**:若要在 chain_live 里覆盖 yt_dlp-only 站(vimeo 等),需 harness 改造挂 yt_dlp(依赖外部 binary,CI 不稳),低优先级。
- 2026-06-30 心跳#27(续):**✅ catalog-mixed(102 站)verify 跑完 + charlie_chaplin .ogv Failed 调查=非 bug(守 L14/L15)**,bug 计数仍 **14**。① breakdown `{Drm:8,Failed:14,SuspectAd:10,Usable:155}` 整体健康:Drm 8=axprod/unified/mux 测试向量(对)、SuspectAd 10=archive `/embed/` HTML 页正确 demote(对)、TED `py.tedcdn.com` 9 个 403=真 S3 `<Code>AccessDenied</Code>` body→Failed(对,bug#7 家族)、社交站(twitter/ig/tiktok/spotify/reddit 等)0 候选=yt_dlp-only 在 generic-only harness 下正确为 0;② **唯一可疑=charlie_chaplin_film_fest.ogv top=Failed**(chain_live 录 500 video/mp4)——探发现 `archive.org/download/...ogv` 302→存储节点 `dn721202.ca`,该节点 500/206 **抖动**(+0/+1s=500,+3/+6/+10s=206,~2-3s 时间尺度恢复),非死节点;③ **坐实重试为何没救**:5xx backoff 仅 `300ms×attempt`(~0.9s 累积),短于 archive 节点恢复尺度;但实测恢复率 cur(300/600ms)=6/8、seconds-scale(1500/3000ms)=7/8——**差异边际且 n=8 噪声内**,且加 backoff 会拖慢所有真死 origin(cap 存在正是为让真死快速终态);④ **判定**:charlie Failed 是真实瞬时(探时节点确实抖),bounded retry 后终态 Failed 可接受,**非误分类**;守 L14/L15 不做投机 backoff 调优(无清晰 repro 证明改动修了误判),记录调查不计 bug。
- **下一步 = P3 续16**:① **escaped-JSON URL 提取审计**(承#26②,catalog 实证优先);② 扩站型(欧日韩挂代理、Rumble/niconico/Twitch-VOD)目标 catalog 100-200 站;③ MIME 别名审计(守纪律);④ 512KiB cap e2e(低优先级);⑤ kind-aware + mac_cms path-aware 上游根因。**可选**:若 archive 节点抖动反复出现且恢复率掉到 <50%,再以更大 n(50+)重测 backoff,有显著差异才改——目前 75% 恢复率不值得动。
- 2026-06-30 心跳#28:**✅ 两个审计均判健康/无 bug(escaped-JSON 提取审计 + peertube 35 实例 verify),bug 计数仍 14**。① **escaped-JSON URL 提取审计(承#26②)**:`decode_url_escapes` 现覆盖 `\/`、`&amp;`、`&#x2F;`、`&#47;`、`&#38;`、`//f`、`&`;扫 7 份真实 HTML 抓包(acfun/bili/streamable/p/page/t/users)只出现 `\/`(8589×)+`&amp;`(322×)两种,均已覆盖;未处理的 `&#x26;`/`&sol;`/`\x2F`/双重编码实测 **0 出现**——守 L14/L15 不投机加,审计闭环**无 gap**;② **peertube 35 实例 verify**:breakdown `{Failed:4,SuspectAd:4,Untested:104,Usable:211}`,**每页 top 都 Usable**(零非-Usable top);4 Failed=tinfoil HLS 变体 404 ENOENT(磁盘真无此文件)+infiniteloop download 端点 500 `{"detail":"Invalid URL","code":"TypeError"}`(PeerTube 自身 bug,可复现),4 SuspectAd=azbyka `-1080.mp4` 现 404(web-video 已删)+emergeheart `/w/` 短链返回 HTML player 页——**全部正确分类**(broken 备选正确 demote,真流正确选为 top);③ **元发现**:连续两轮(#27/#28)审计无 bug,generic+verify 在 archive/peertube/mixed 三大 corpus 已稳健;**bug-finding 的瓶颈已转为 harness 覆盖**——chain_live 只接 direct+generic,所有 yt_dlp/browser-only 站(社交/SPA/Rumble/niconico/Twitch)在 harness 下恒 0 候选(L22),再加同形态站(archive 影片/peertube 实例)是 padding 低 ROI;④ 诚实记账:本轮无代码改动,两个审计闭环 + 确认 corpus 健康是有效产出。
- **下一步 = P3 续17**:① **评估 harness 扩展挂 yt_dlp**——这是当前 bug-finding 的真实前沿(production Auto 链 `...generic→yt_dlp→you_get→lux→streamlink→browser`,但 chain_live 只测 direct+generic);挂 yt_dlp 能覆盖社交/SPA 站的 classify_probe/verify 路径,但依赖外部 binary(CI 不稳)——评估代价 vs 收益,或单开一个 `RK_CHAIN_YTDLP=1` gated 变体测试;② 若不扩 harness,则找**generic 可提取的新基础设施形状**(非 archive/peertube 同形态):MediaCMS/Owncast/Funkwhale/Kaltura-embed/JW-Player-embed/Brightcove 等 server-rendered 媒体 URL 站;③ MIME 别名审计(守纪律);④ 512KiB cap e2e(低优先级);⑤ kind-aware + mac_cms path-aware 上游根因。**注**:catalog 已 ~237 行(去重后 ~150+ 唯一站),100-200 目标基本达成,**不再 padding 同形态站**。
- 2026-06-30 心跳#29(ultracode):**✅ ultracode workflow 扇出发现 5 类新 server-software 形状 + 跑 chain_live 验证,bug 计数仍 14**。① **workflow `rk-discover-serversoftware`**(5 opus general-purpose agent parallel,中途 session 退出→resume 重跑,54 URL):MediaCMS/MediaGoblin/Castopod/Owncast/JW-Brightcove-Kaltura/public-broadcaster;代理验活 45→43 live(C-SPAN 403 bot-block 丢弃);建 `catalog-serversoftware.txt`(41 站);② **chain_live 结果 `{Usable:41}`**——分两组:**(A)新形状 generic 正确提取**=MediaGoblin libreplanet 12/12(`<video><source>`.webm,cands=1)、Castopod 8/8(`og:audio`→.mp3 enclosure,cands=3-4,注意 `\/` 转义 L21 已处理)、Owncast 在线 2(retrostrange/swimrewind,HLS),全真 Usable 已坐实;**(B)cands=0 但正确**=MediaCMS 11(demo.mediacms.io/cinemata,**API-gated**:页 JSON-LD VideoObject 只有 `embedUrl`→JS-shell embed 页无静态媒体、无 `contentUrl`,真媒体在 `/api/v1/media/<id>` JSON,generic 读静态 HTML 故 0,**正确**,同 yt_dlp-only 类)、JW/Brightcove/Kaltura embed 5(JS-SPA player shell)、Owncast 离线 3;③ **关键发现(非 bug)**:**MediaCMS 是未来 site-specific extractor 的好候选**——它 API 干净(`/api/v1/media/<id>` 返回 `encodings_info`/`hls_info`/`original_media_url` JSON),且是高基数开源平台(很多公共实例);但这不是 generic 缺陷(generic 设计就读静态 HTML),归入"需专用 extractor"类,不计 bug;④ 诚实记账:本轮无代码改动,但**确认 generic 在 4 类新 server-software 形状的提取行为**(2 类正确提取 Usable、2 类正确 0-候选),且为 catalog 增 22 个真实新形状站 + 定位 MediaCMS 为专用 extractor 候选。
- **下一步 = P3 续18**:① **MediaCMS site-specific extractor 评估**——它 API 形状清晰(`/view?m=<id>` → `/api/v1/media/<id>` JSON 含 hls_info+encodings_info),是 generic 之外唯一确认的"可提取但需专用逻辑"形状;评估是否值得在现有 extractor 链加一个 MediaCMS 探测(host/path 模式匹配 `?m=` + JSON-LD `@type:VideoObject` 且 embedUrl 含 `/embed?m=` → 取 `/api/v1/media/<id>`),或归入 yt_dlp 兜底范畴;② 评估 harness 挂 yt_dlp(`RK_CHAIN_YTDLP=1` gated);③ MIME 别名审计;④ 512KiB cap e2e。**注**:catalog 现 7+1=8 个 catalog 文件,去重 ~210 唯一站,形状覆盖已广(archive/commons/peertube/MediaGoblin/Castopod/Owncast/DRM-vectors/playerconfig)。
- 2026-06-30 心跳#30(ultracode):**✅ 实现 MediaCMS 提取支持(bug/feature #6),0→Usable,零回归**。承#29 评估结论,确认 MediaCMS 在整条 production 链未被覆盖(yt-dlp 无专用 extractor、其 generic 也靠 og:video/JSON-LD contentUrl,MediaCMS 两者皆无),先 curl 坐实全链可提取:`/view?m=<id>`→`/api/v1/media/<id>` JSON→`hls_info.master_file`(200 valid HLS master)+`encodings_info.*.h264.url`(206 video/mp4),均真可播。**实现选 Option B(扩 generic 的 `detect_known_apis`,非新建 host-matched extractor)**——理由:MediaCMS 域名任意(demo.mediacms.io/cinemata.org/...),generic 已 fetch 页面 + 有 `api_endpoints`/`fetch_api_media` 通路(原为 PeerTube 形状探测建),复用避免双 fetch + 假阳 `matches()`。**两处改 generic.rs**:(1)`detect_known_apis` 加 MediaCMS:path `/view`或`/embed` + query `m=<id>` → queue `/api/v1/media/<id>`(形状匹配,域名无关,与 PeerTube 并列);抽出 `push_api_endpoint` helper;(2)**`harvest_json_urls` 修根因 bug**:原 `s.starts_with("http")` 守卫**丢弃所有根相对媒体 URL**,而 MediaCMS API 返回的正是 `/media/hls/<hash>/master.m3u8` 这类相对路径——加 `else if s.starts_with('/')` 分支,在 `classify_url` 已确认真媒体扩展时 `base.join()` 解析(这是通用修复,不止 MediaCMS)。**验证**:5 单测全绿(含 PeerTube 无回归)+ chain_live `catalog-serversoftware` breakdown 从 `{Usable:41}`→`{Failed:2,Untested:96,Usable:127}`,**MediaCMS 11 页全 0→17-20 候选 top=Usable**(+86 Usable),**全 catalog 零 top 非-Usable(零回归)**,2 Failed = MediaCMS `iframes.m3u8`(I-frame trick-play 缩略图轨,非主播放轨,verify 正确 demote 选 master 为 top),仍 0 候选的只剩 JS-SPA(Brightcove/Kaltura embed)+ 离线 Owncast(L24 layer-c,正确)。23 个 generic 单测全过。
- **下一步 = P3 续19**:① **真 bug 计数 14→15**(MediaCMS 提取 + 根相对 JSON URL 解析,后者是通用根因);② 评估 harness 挂 yt_dlp(`RK_CHAIN_YTDLP=1` gated)覆盖社交/SPA 链(剩余前沿);③ **Kaltura/Brightcove embed 也是 API-gated 候选?**——它们是 JS-SPA player(L24 layer-c),但 Kaltura 有公开 `/api_v3` + Brightcove 有 Playback API(需 policy key),评估 ROI(比 MediaCMS 复杂、需 token,可能归 browser/yt_dlp 兜底更合理);④ MIME 别名审计;⑤ 512KiB cap e2e。**注**:`harvest_json_urls` 根相对解析这个通用修复,可能让之前 cands=0 的其它 JSON-API 站(非 MediaCMS)也受益,值得回扫 playerconfig/manifest catalog 看有无新 Usable。
- 2026-06-30 心跳#31(ultracode):**✅ 回扫 playerconfig/manifest 验证#30 通用修复范围 + 深挖 Odysee SuspectAd,判定非 bug,计数仍 15**。① **回扫结论:#30 根相对修复是 MediaCMS 专属赢,无外溢**——playerconfig 17 站 breakdown `{SuspectAd:5,Usable:4}` 与修复前一致(Vimeo/Dailymotion 是纯 JS-SPA layer-c 需 browser、streamable 已 Usable);manifest 7 站全是直链 manifest(direct/generic 已覆盖,非 JSON-API)。诚实记账:通用修复未解锁新站,符合预期(只有返回相对媒体路径的 JSON-API 才受益)。② **Odysee 5 视频全 top=SuspectAd 深挖**:generic 从页面 bare-URL 抓到 `player.odycdn.com/api/v3/streams/free/<name>/<claim>/<sd>.mp4`,该 CDN 端点恒 **401 `this content cannot be accessed at the moment`**(+Referer 翻 429)——这正是 verify.rs:309-315 既有 transient-throttle 信号,**SuspectAd 是设计内正确分类**(非终态、可恢复)。**真 playable URL 是另一个形状** `player.odycdn.com/v6/streams/<claim>/<sd>.mp4`(实测 206 video/mp4 真 ISO MP4),只能 POST `api.na-backend.odysee.com/api/v1/proxy?m=get`(LBRY jsonrpc)拿到。③ **判定非 bug**:(a)yt-dlp 有成熟 upstream LBRY extractor 用的正是这个 proxy API,production `generic→yt_dlp` 链覆盖 Odysee;(b)**关键正确性属性**:SuspectAd 候选**不触发 short-circuit**(mod.rs:207-212 只在确认 Usable 时 break),所以 generic 抓到的 401 诱饵 URL **不会挡住 yt_dlp 运行**——诱饵被正确 demote 且链继续,yt_dlp 拿到真 `/v6/` 流。harness 显示 SuspectAd 仅因 chain_live 没接 yt_dlp(L23 既知)。④ 诚实记账:本轮无代码改动,确认#30 修复范围 + 坐实 Odysee 在 production 被覆盖 + 提炼"诱饵 URL 被 SuspectAd 安全收容、不阻断下游 extractor"这一非显然正确性属性(L26)。
- **下一步 = P3 续20**:① **harness 挂 yt_dlp 已成最高优先**——连续多轮(#29-#31)发现的"未覆盖"站(MediaCMS 除外的 Odysee/Vimeo/Dailymotion/JW/Brightcove/Kaltura)在 production 全靠 generic 之后的 yt_dlp/browser 链,但 chain_live 测不到,导致无法验证那段链的 classify_probe/verify 真实行为;建 `RK_CHAIN_YTDLP=1` gated 变体(检测 binary 缺失则 skip,CI 安全),让 catalog 能跑完整 Auto 链;② Kaltura `/api_v3` extractor 评估(承#30);③ MIME 别名审计;④ 512KiB cap e2e。**注**:catalog 形状覆盖已足广,bug-finding 的下一个真前沿明确是 harness 把 yt_dlp 接进来。
- 2026-06-30 心跳#32(ultracode):**✅ 把 yt_dlp 接进 chain_live harness(P3 续20 最高优先项落地),Vimeo/Dailymotion 0→Usable、Odysee SuspectAd→Usable,零回归**。① **实现**:`mod.rs` 测试 harness 三处改:(a)新 helper `chain_yt_dlp_probe()` 从 `RK_CHAIN_YTDLP` env(yt-dlp 二进制路径)建 `YtDlpProbe`(60s timeout/16MiB cap),env 未设或路径不存在则返 `None`;(b)`ctx_with_discovery` 的 `yt_dlp` 字段从 `None` 改 `chain_yt_dlp_probe()`;(c)`run_chain` 无条件加 `YtDlpExtractor`(`matches()` 只在 `ctx.yt_dlp.is_some()` 才 fire,故未设 env 时 no-op,镜像 production Auto 链 `Direct→Generic→YtDlp`)。**CI 安全**:107 个默认单测全过(env 未设 → yt_dlp=None → YtDlpExtractor no-op,零回归)。② **装 yt-dlp**:home-cloud `~/bin/yt-dlp` 独立二进制 2026.06.09(走代理下),yt-dlp 自读 `ALL_PROXY`/`HTTPS_PROXY` 无需改接线。③ **验证(playerconfig 17 站,接 yt_dlp)**:breakdown `{SuspectAd:5,Usable:4}`→`{SuspectAd:5,Untested:84,Usable:74}`(**+70 Usable**)——**Vimeo 全 6 站 0→Usable**(13-31 候选,原 layer-c JS-SPA)、**Dailymotion 2 站 0→Usable**、**Odysee 4/5 SuspectAd→Usable**(yt_dlp lbry extractor 拿到真 `/v6/streams/<claim>/<sd>.mp4` HLS,坐实 L26 诱饵旁路);仍非-Usable 的:2 Odysee 是 ended livestream(yt-dlp 报 `This stream is not live`,正确 0)、generic 抓的 `api/v3/streams/free` 401 诱饵仍作低分 SuspectAd 候选(L26,正确)、个别站经 SOCKS 偶发 yt-dlp 超时(网络抖动非 bug)。④ README 加"接 yt-dlp 跑完整 Auto 链"章节。
- **下一步 = P3 续21**:① **用接了 yt_dlp 的 harness 回扫所有 catalog**——尤其 catalog-mixed(102 站,含 instagram/tiktok 等 12 个原 0 候选 JS 壳社交站)、catalog-serversoftware(JW/Brightcove/Kaltura embed),看 yt_dlp 把多少原 0/SuspectAd 翻成 Usable,这是验证 production 链真实覆盖率的首次机会;② 从回扫里挑 yt_dlp **也**搞不定的站(真前沿:需 browser/cookie/DRM)深挖;③ Kaltura `/api_v3` extractor 评估(若 yt_dlp 已覆盖则降优先);④ MIME 别名审计;⑤ 512KiB cap e2e。**注**:harness 现可跑完整 production Auto 链(Direct→Generic→YtDlp),bug-finding 视野第一次覆盖到 generic 之后那段——之前所有"non-bug,production 有 yt_dlp 兜底"的判断现在都能实证。
- 2026-06-30 心跳#33(ultracode):**✅ 修真 bug #16——verify 探测丢弃 extractor 抓到的 replay headers(L28)**。① **回扫**:接 yt_dlp 跑 catalog-mixed(102 站)得 `{Drm:16,Failed:20,NeedsProfile:8,SuspectAd:10,Untested:2,Usable:152}`;**TikTok 0→cands=10**(yt_dlp 提到 10 个签名 CDN URL,top=NeedsProfile)、TED 12 站全 top=Usable(yt_dlp HLS)、仍 0 的是 instagram/twitter/spotify/reddit/pinterest/linkedin/discord/cnn(纯登录墙 c2,yt_dlp 也搞不定,正确)。② **挖到真 bug**:深挖 TikTok NeedsProfile 时发现 yt_dlp 的每个 format 带 `http_headers`(Referer/UA/Accept)+`cookies`,external_probe 已把**非敏感**那批(UA/Accept/Accept-Language/Referer/Origin allowlist)存进 `metadata_json.download_headers`(敏感的 cookie/auth/x-* 排除并置 requires_profile),**但 verify 的 `probe()` 只用固定 `VERIFY_UA` 裸探,从不回放 download_headers**——CDN 若对 Referer/UA 签名校验,verify 拿 403→误判 NeedsProfile/Failed,即便 URL 用 extractor 已学到的 header 就能播。③ **修复**:新 `probe_capped_with_headers()`(老 `probe`/`probe_capped` 委托空 header,行为不变)+ `candidate_replay_headers()` 从 `metadata_json.download_headers` 取回放头;`verify_one` 5 处探测调用全部改为传 `&replay_headers`(含 UA 走 client builder、Range 由 body-cap 独占、跳过这俩避免覆盖)。④ **实证(playerconfig 重跑)**:noclip(13)、apple-is-tracking(14)两个 Odysee 从 `cands=1 SuspectAd`→`cands=2/6 Usable`——verify 回放 yt_dlp 头后 `/v6/` 流验活成功,坐实修复价值;breakdown `{SuspectAd:5,Usable:74}`→`{SuspectAd:8,Usable:77}`。⑤ 110 单测过(+3:download_headers 提取/空/非字符串跳过)。
- **下一步 = P3 续22**:① **catalog-serversoftware/serversoftware2 接 yt_dlp 回扫**(JW/Brightcove/Kaltura embed,#29 layer-c 判"需 browser"的,验证 yt_dlp 是否够——Vimeo/Dailymotion 已证 c 应再分 c1=yt_dlp 能搞 / c2=真需 browser);② **找 Referer-gated CDN 做 replay-header e2e 实证**(本轮 TikTok 需完整 browser session 非 demonstrator、Vimeo/TED 是自签 URL 不验 Referer——需找真正只缺 Referer/UA 的站把 #16 的价值钉死);③ 真 c2 前沿(login-wall 社交站)记录但不强攻;④ MIME 别名审计;⑤ 512KiB cap e2e。**注**:#16 让 verify 的探测保真度对齐 extractor 的请求上下文,是 verification 闭环的一处结构性补强。
- 2026-06-30 心跳#34(ultracode):**纪律轮——serversoftware 接 yt_dlp 回扫,clean pass,L27 的 c1 重分类实证落地(L29,非 bug 不计数)**。① 跑 catalog-serversoftware(42 站)得 `{Usable:224,Untested:13}`,**零 Failed/SuspectAd**;6 个 0 候选全经 yt_dlp 直测确认是**源头死内容/正确**:chiptune/indiebeat/gamesmaster(32/34/36)是裸首页无具体媒体、Brightcove(38)demo videoId 404、kaltura.com(39)partner_id 1851962 被 block、edu.mediaspace(41)entry id invalid。② **c1 重分类实证**:#29 判 layer-c「需 browser」的 JW/Kaltura/Brightcove,本轮证全是 **c1(yt_dlp 覆盖)**——JW preview(37)cands=2 Usable、两个有效 Kaltura mediaspace(40/42)Usable、illinois(40)Usable;只在内容已死时 0 候选(与 browser 无关)。**唯一真 c2 仍是 login-wall 社交站(instagram/twitter/… 见 #33)**。③ catalog 标注 3 个 rot 死链(注释保留作 dead-content anchor,0 候选是正确不是回归)。
- **下一步 = P3 续23**:① **找 Referer-gated CDN 把 #16 replay-header 价值钉死**(仍欠一个"只缺 Referer/UA"的纯 demonstrator——TikTok 需 browser session、Vimeo/TED 自签;候选方向:bilibili/部分日系站/带 hotlink 防护的自建 CDN);② **回扫 catalog-peertube(35)+ catalog-archive(41)接 yt_dlp**,这两个大 corpus 还没在 yt_dlp 全链下跑过,可能暴露 generic↔yt_dlp 交接的新行为;③ 真 c2 social 前沿记录不强攻;④ MIME 别名审计;⑤ 512KiB cap e2e。**注**:连续 serversoftware clean pass 说明 server-rendered/embed 这条线已被 generic+yt_dlp 充分覆盖,bug ROI 低;前沿要么是 Referer-gated CDN(验 #16),要么是还没接 yt_dlp 回扫的老 corpus。
- 2026-06-30 心跳#35(ultracode):**✅ 钉死 #16——加 pximg Referer-hotlink live 回归测试,实证 verify 回放 download_headers 翻转判定(L28 补完)**。① **找到纯 demonstrator**:bilibili 经美国 SOCKS 出口被反爬 412/地域拦(数据中心 IP),换 **i.pximg.net**——pixiv 图床的 Referer 防盗链**不挑出口 IP**,任何主机裸 GET→403 text/html、带 `Referer: pixiv.net`→200 image/jpeg,机制确定且多年稳定;② **实证**:用 pixiv 公开 ranking API 取当前真实作品图 URL(pximg 路径含日期会腐烂,故运行时取),同一 URL **裸探 403 / 带 Referer 200+179738B**(直连 no-proxy 复现,匹配 verify 的 `.no_proxy()` 探测面);③ **加 live 测试** `verify_live_pximg_referer_gate_replays_download_headers`(运行时 fetch 新 URL→自包含不腐烂):(a)无 download_headers 裸探→`NeedsProfile`/403(修复前的假判),(b)candidate 带 `download_headers.Referer` →verify 回放→`Usable`/200 image/jpeg;④ **跑通**:两条断言全过,坐实 #16 不再是"理论上对"而是有代码级回归保护;⑤ 110 lib 单测过(+1 ignored = pximg live)。**至此 #16 从"已上线未证明"→"已证明+有回归 fixture"**。
- **下一步 = P3 续24**:① **回扫 catalog-peertube(35)+ catalog-archive(41)接 yt_dlp 全链**——这两个最大的老 corpus 还没在 yt_dlp 接入后跑过,是目前唯一可能暴露 generic↔yt_dlp 交接新行为的未探区域(ROI 最高的下一步);② 真 c2 social 前沿(instagram/twitter 登录墙)记录不强攻;③ MIME 别名审计(守 L14/L15 纪律,catalog 实证优先);④ 512KiB cap e2e(低优先级)。**注**:verification 闭环的几条已知松动(#16 未证明)已全部收紧;若 peertube/archive 回扫也 clean,则 server-rendered+embed+direct 三条线全覆盖,该认真考虑收尾(把 RK_VERIFY_ENABLED 从默认 OFF 推向灰度 ON + 总结文档)而非继续刷站。
- 2026-07-03 心跳#36(ultracode):**纪律轮——peertube+archive 接 yt_dlp 全链回扫,双 corpus clean pass,闭合 P3 续24①(L31,非 bug 不计数)**。① **回扫**(远端 home-cloud 代理已从 socks5 1080 迁到 mixed 端口 **7890**,http+socks5h 皆通,yt-dlp 走 `ALL_PROXY=socks5h://127.0.0.1:7890`):peertube 35 站 `{Failed:4,SuspectAd:6,Untested:31,Usable:282}`、**top=Usable 35/35**;archive 41 站 `{SuspectAd:6,Usable:109}`、**top=Usable 41/41**;两个 `chain_live_catalog` 均 `1 passed 0 failed`。② **逐个核验非-Usable 全部正确(catalog rot,非 bug)**:peertube 的 tinfoil-hat 404 m3u8 + infiniteloop 500 是已知死内容(续11 已锚)、tube.azbyka.ru 的 `-720.mp4` web-video 变体 curl 404+**裸 yt-dlp `FORMATS=NA`**(源头死,页 HLS 候选才是 Usable top)、emergeheart 的 text/html 是 embed 壳(裸 yt-dlp 也 NA);archive 的 `/embed/` text/html 全是 player 壳,真媒体候选排 Usable top。③ **意义**:这是**最后一块未在 yt_dlp 全链下跑过的老 corpus**,clean 说明 direct+generic+embed(serversoftware #34)+server-rendered/联邦(peertube/archive)四条线在完整 `Direct→Generic→YtDlp` 链下全覆盖,零 wrong-top。**bug-finding 前沿已收敛到真 c2(login-wall 社交站,yt_dlp 也搞不定)——刷站 ROI 见底,正式进入收尾评估**(RK_VERIFY_ENABLED 灰度决策 + 总结文档),等用户拍板。
- **下一步 = 收尾(P4)**:① **verify 灰度决策**——起草 `RK_VERIFY_ENABLED` 从默认 OFF 推向灰度 ON 的方案(风险/回滚/按 env 百分比或按路由灰度),**交用户拍板**,不擅改 production 默认;② 环路总结文档(16 真 bug + 覆盖矩阵 + c1/c2 分层 + verify 闭环状态);③ 若维持刷站:仅剩真 c2 social 前沿(需 browser/cookie,记录不强攻);④ 长期挂账(低优先,守 L14/L15 纪律):512KiB cap e2e(需本地 fixture)、MIME 别名审计、kind-aware audio↔video 语义、mac_cms `.contains(".m3u8")` path-aware、per-host 探测节流间隔。**注**:代理端口从 1080 迁 7890,README 的 yt-dlp recipe 与 L27 记的 `socks5h://127.0.0.1:1080` 已过时,收尾时应更新。
